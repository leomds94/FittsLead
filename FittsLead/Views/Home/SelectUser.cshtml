@*@model FittsLead.Models.MainViewModel

<div class="demo-section k-content">
    @(Html.Kendo().Chart(Model.SpeedTargets)
        .Name("chart1")
        .Title("Velocidade do ponteiro")
        .Legend(legend => legend
            .Position(ChartLegendPosition.Bottom)
        )
        .Series(series =>
        {
            foreach (FittsLead.Models.SpeedTarget t in Model.SpeedTargets)
            {
                series.Line(t.SpeedPoints);
            }
        })
        .ValueAxis(axis => axis.Numeric()
            .Labels(labels => labels.Format("{0}"))
            .MajorUnit(1000)
            .Line(line => line.Visible(false))
            .Min(Model.SpeedTargets.Last().MinVel)
            .Max(Model.SpeedTargets.Last().MaxVel)
        )
        .Tooltip(tooltip => tooltip
            .Visible(true)
            .Format("{0}")
        )
    )
</div>

<div class="demo-section k-content">
    @(Html.Kendo().Chart(Model.SpeedTargets)
        .Name("chart2")
        .Title("Dist�cia do alvo")
        .Legend(legend => legend
            .Position(ChartLegendPosition.Bottom)
        )
        .Series(series =>
        {
            foreach (FittsLead.Models.SpeedTarget t in Model.SpeedTargets)
            {
                series.Line(t.DistFromTarget);
            }
        })
        .ValueAxis(axis => axis.Numeric()
            .Labels(labels => labels.Format("{0}"))
            .Line(line => line.Visible(false))
            .Min(Model.SpeedTargets.Last().MinDist)
            .Max(Model.SpeedTargets.Last().MaxDist)
        )
        .Tooltip(tooltip => tooltip
            .Visible(true)
            .Format("{0}")
        )
    )
</div>

<div class="demo-section k-content">
    @(Html.Kendo().Chart(Model.SpeedTargets)
        .Name("chart3")
        .Title("Dist�cia do Centro")
        .Legend(legend => legend
            .Position(ChartLegendPosition.Bottom)
        )
        .Series(series =>
        {
            List<double> distCenter = new List<double>();
            foreach (FittsLead.Models.SpeedTarget t in Model.SpeedTargets)
            {
                distCenter.Add(t.DistFromCenter);
            }
            series.Line(distCenter);
        })
        .ValueAxis(axis => axis.Numeric()
            .Labels(labels => labels.Format("{0}"))
            .Line(line => line.Visible(false))
        )
        .Tooltip(tooltip => tooltip
            .Visible(true)
            .Format("{0}")
        )
    )
</div>

<div class="demo-section k-content">
    @(Html.Kendo().Chart(Model.SpeedTargets)
        .Name("chart1")
        .Title("Velocidade do ponteiro")
        .Legend(legend => legend
            .Position(ChartLegendPosition.Bottom)
        )
        .Series(series =>
        {
            foreach (FittsLead.Models.SpeedTarget t in Model.SpeedTargets)
            {
                series.Line(t.DistDisplacement);
            }
        })
        .ValueAxis(axis => axis.Numeric()
            .Labels(labels => labels.Format("{0}"))
            .MajorUnit(1000)
            .Line(line => line.Visible(false))
        )
        .Tooltip(tooltip => tooltip
            .Visible(true)
            .Format("{0}")
        )
    )
</div>*@